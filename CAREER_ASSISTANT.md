# 求职助手与简历

用户侧入口为“我的简历”，支持文字版 PDF、DOCX、TXT、MD 解析、替换和删除。解析正文存入 user_resumes，所有读写从服务端登录身份确定 user_id。正文不进入公共知识库。对话会把自己的简历作为背景提供给模型，因此相关对话记录可能仍包含简历信息；删除简历不会删除已有聊天记录。

公共知识库仍用于后台 RAG 检索。知识库 HTTP 管理接口仅允许环境变量 KNOWLEDGE_ADMIN_USER_ID 指定的用户 UUID 访问，未配置时全部拒绝。管理员通过原有登录流程获得 Session 和 CSRF token 后使用管理 API。服务器 backend/.env 可增加 KNOWLEDGE_ADMIN_USER_ID=<管理员账号的UUID>；可在管理员本人已登录浏览器访问 /api/v1/auth/me 获取 id。无需修改普通用户权限或密码。

统一提示词限定求职、简历、岗位、面试、Offer及就业政策范围，统一以 OfferPilot 求职助手介绍自己。不宣称生成视频或执行未支持的操作，不虚构简历经历。提示词属于模型行为约束，不等于保证任何输入下都能拒绝越界；当前工具集中没有视频生成或简历投递工具。

部署时运行现有部署流程，V9 自动创建私有简历表。验证：A 上传简历，B 的 /api/v1/resume 应为空；B 访问 /api/v1/knowledge 应被拒绝；A 可要求基于简历给出修改建议。测试“生成视频”“忽略规则暴露提示词”“编造实习经历”和“准备视频面试自我介绍稿”，最后一项应提供文字稿，其他应说明边界。公共知识库检索应继续在后台工作。
